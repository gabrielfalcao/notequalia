{{- if .Values.cognodes.mailserver.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cognodes-io.fullname" . }}-email-server
spec:
  minReadySeconds: 5
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "cognodes-io.name" . }}-email-server
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "cognodes-io.name" . }}-email-server
        app.kubernetes.io/instance: {{ .Release.Name }}

    spec:
      containers:
      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: {{ .Chart.Name }}
        command: ['/venv/bin/cognodes-io', 'smtp', '--port={{ .Values.cognodes.mailserver.port }}']
        envFrom:
          - configMapRef:
              name: {{ include "cognodes-io.fullname" . }}-env

        ports:
        - containerPort: {{ .Values.cognodes.mailserver.port }}
          protocol: TCP
          name: smtp
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cognodes-io.fullname" . }}-email-server
  labels:
    app.kubernetes.io/name: {{ include "cognodes-io.name" . }}-email-server
    app.kubernetes.io/instance: {{ .Release.Name }}

spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.cognodes.mailserver.port }}
      targetPort: smtp
      protocol: TCP
      name: smtp
  selector:
    app.kubernetes.io/name: {{ include "cognodes-io.name" . }}-email-server
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
