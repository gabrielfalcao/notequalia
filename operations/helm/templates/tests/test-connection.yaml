apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cognodes-io.fullname" . }}-test-connection"
  labels:
{{ include "cognodes-io.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cognodes-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
